2019,7.24:
	*熟悉了IDEA的编辑配置；
	*学会了三目运算符的运用；
	*自己通过写TernaryOperator.java加深了数组初始化的理解，即不能写成如下形式：
  	int[] t;
	t[0] = 1;
	t[1] = 2;
	......
即不能初始化一个未知大小的数组，除非这么写：int[] t = {1,2};否则只能写成：
int[] t = new int[size];或者分开写：int[] t; t = new int[size];(size已知)
	*第一次按照java源码的格式书写代码感觉很好，以后继续保持。
