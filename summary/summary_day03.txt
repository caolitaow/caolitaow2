2019,7.25:
	源程序--编译-->字节码--运行-->jvm
	jvm-->类加载-->字节码验证-->翻译-->硬件
	字节码验证：
		字节码是否和jvm匹配
		程序是否破坏系统的完整性
		栈的溢出
		参数类型
		类型转换
		打可以运行的jar包：
		jar -cvfe h.jar com.briup.ch01.HelloWorld com
		运行jar包：
   		java -jar xx.jar
		
	文档注释：
		javadoc -d [指定注释生成的目录] [sourcefiles]|[@file]
		[@file]存着要生成注释文档的源程序的路径 用来一次性生成多个不同路径下源程序的注释文档
	标识符命名规则：
                    不能以数字开头作标识符，且不能用关键字和保留字作标识符
		注意：{		
		int $99; String _ab12;vaild 
		int 3t;illegal
		保留字：goto const(以后可能变成关键字)
		中文可作为标识符
                java源码中的类即引用类名可作为标识符
		......
		}
	单个字符类型的字符的表示：
		*可以用"\u0060"表示单个字符类型（即用unicode形式）
		‘字符’
		ASCII码值
	类型转换：
		*整型之间，浮点型之间：显式转换--宽变窄；隐式转换--窄变宽
		*整型到浮点型--隐式转换；浮点型到整型--显式转换
	属性--filed字段可理解成属性
拓展：
	windows下安装了git及git的gui工具TortoiseGit并汉化并推送了一个项目
     *	 += 自动强制类型转换与 = 不同 
	
